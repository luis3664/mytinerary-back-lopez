import { getAllCountries, createAllCountries } from '../services/countriesServices.js'

const countriesControllers = {
    getAllCountries,
    createAllCountries
};

export default countriesControllers;